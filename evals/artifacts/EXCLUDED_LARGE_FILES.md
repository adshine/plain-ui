# Large artifacts kept out of git

- Playwright webm recordings and extracted frame directories from temporal discovery
- Full raw screenshot dumps from the initial playground sweep

Regenerate via harnesses under `/tmp/skills-eval/` (ephemeral) using docs on `:4321` and registry fixtures on `:4391`.

Blocking-gate evidence that *is* committed lives in `evals/artifacts/gates-2026-08-25/`.
